# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Credenciales. `*.local` ya cubre .env.local, pero no un .env pelado ni las
# variantes por entorno — y acá adentro viven los access tokens de Mercado Pago
# y la service_role de Supabase. .env.example SÍ se versiona: no tiene valores.
.env
.env.*
!.env.example
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Planning docs (RFC, SDD, ADRs, backlog) — local-only por ahora
docs/

# Skills tooling artifacts (npx skills add)
.agents/
skills-lock.json

# Skills tooling artifacts (locks locales)
.claude/

# Servidores MCP: qué base o qué servicio conecta cada uno depende de la
# máquina y de quién esté trabajando. No es configuración del proyecto.
.mcp.json
